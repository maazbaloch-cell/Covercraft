"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { useRouter } from "next/navigation";

export default function CustomerDashboard() {
  const router = useRouter(); const [data, setData] = useState<any>(null); const [orders, setOrders] = useState<any[]>([]); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const [password, setPassword] = useState({ currentPassword: "", newPassword: "" });
  const load = async () => { try { const [profile, orderResponse] = await Promise.all([fetch("/api/customer/profile", { credentials: "include" }), fetch("/api/customer/orders", { credentials: "include" })]); if (profile.status === 401) return router.replace("/account"); const profileBody = await profile.json(); if (!profile.ok) return setError(profileBody.error || "Unable to load your account."); setData(profileBody.customer); if (orderResponse.ok) setOrders((await orderResponse.json()).orders || []); } catch { setError("Unable to load your account. Please try again."); } };
  useEffect(() => { void load(); }, [router]);
  const saveProfile = async (event: FormEvent) => { event.preventDefault(); setSaving(true); setMessage(""); setError(""); try { const response = await fetch("/api/customer/profile", { method: "PATCH", headers: { "Content-Type": "application/json" }, credentials: "include", body: JSON.stringify({ name: data.name, phone: data.phone || "" }) }); const body = await response.json(); if (!response.ok) setError(body.error || "Unable to save profile."); else { setData({ ...data, ...body.customer }); setMessage("Profile saved."); } } finally { setSaving(false); } };
  const changePassword = async (event: FormEvent) => { event.preventDefault(); setSaving(true); setMessage(""); setError(""); try { const response = await fetch("/api/customer/profile", { method: "PUT", headers: { "Content-Type": "application/json" }, credentials: "include", body: JSON.stringify(password) }); const body = await response.json(); if (!response.ok) setError(body.error || "Unable to change password."); else { setPassword({ currentPassword: "", newPassword: "" }); setMessage("Password updated."); } } finally { setSaving(false); } };
  const saveAddress = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); setSaving(true); setError(""); try { const response = await fetch("/api/customer/addresses", { method: "POST", headers: { "Content-Type": "application/json" }, credentials: "include", body: JSON.stringify({ label: form.get("label"), recipient: form.get("recipient"), phone: form.get("phone"), line1: form.get("line1"), line2: form.get("line2"), city: form.get("city"), isDefault: true }) }); const body = await response.json(); if (!response.ok) setError(body.error || "Unable to save address."); else { setData({ ...data, addresses: [body.address, ...data.addresses] }); event.currentTarget.reset(); setMessage("Address saved."); } } finally { setSaving(false); } };
  const logout = async () => { await fetch("/api/customer/logout", { method: "POST", credentials: "include" }); router.replace("/"); router.refresh(); };
  if (!data) return <div className="mx-auto max-w-6xl px-6 py-12 text-slate-600">{error || "Loading your account…"}</div>;
  return <main className="mx-auto max-w-6xl px-6 py-10"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-violet-700">Your CoverCraft account</p><h1 className="mt-2 text-3xl font-black text-slate-950">Hello, {data.name.split(" ")[0]}.</h1></div><button onClick={logout} className="text-sm font-bold text-violet-700">Sign out</button></div>{(message || error) && <p className={`mt-5 rounded-xl px-4 py-3 text-sm ${error ? "bg-rose-50 text-rose-700" : "bg-emerald-50 text-emerald-700"}`}>{error || message}</p>}<div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-black">Profile</h2><form onSubmit={saveProfile} className="mt-4 space-y-3"><input value={data.name} onChange={(e) => setData({ ...data, name: e.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2.5" placeholder="Full name"/><input value={data.email} disabled className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-slate-500"/><input value={data.phone || ""} onChange={(e) => setData({ ...data, phone: e.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2.5" placeholder="Phone number"/><button disabled={saving} className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-60">Save profile</button></form></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-black">Change password</h2><form onSubmit={changePassword} className="mt-4 space-y-3"><input type="password" value={password.currentPassword} onChange={(e) => setPassword({ ...password, currentPassword: e.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2.5" placeholder="Current password"/><input type="password" minLength={12} value={password.newPassword} onChange={(e) => setPassword({ ...password, newPassword: e.target.value })} className="w-full rounded-xl border border-slate-200 px-3 py-2.5" placeholder="New password (12+ characters)"/><button disabled={saving} className="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-60">Update password</button></form></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-black">Saved addresses</h2>{data.addresses.length ? <div className="mt-4 space-y-3">{data.addresses.map((address: any) => <div key={address.id} className="rounded-xl bg-slate-50 p-3 text-sm"><b>{address.label}</b><p>{address.recipient} · {address.phone}</p><p className="text-slate-500">{address.line1}, {address.city}</p></div>)}</div> : <p className="mt-3 text-sm text-slate-500">No saved addresses yet.</p>}<form onSubmit={saveAddress} className="mt-5 grid gap-2"><input required name="label" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="Address label"/><input required name="recipient" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="Recipient name"/><input required name="phone" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="Phone"/><input required name="line1" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="Address"/><input name="line2" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="Apartment / area (optional)"/><input required name="city" className="rounded-xl border border-slate-200 px-3 py-2" placeholder="City"/><button disabled={saving} className="w-fit rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white">Save address</button></form></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-black">Recent orders</h2>{orders.length ? <div className="mt-4 space-y-3">{orders.slice(0, 5).map((order) => <Link key={order.id} href={`/track?order=${order.orderNumber}`} className="block rounded-xl bg-slate-50 p-3 text-sm"><b>{order.orderNumber}</b><span className="float-right font-bold">{order.status.replaceAll("_", " ")}</span><p className="mt-1 text-slate-500">Rs. {(order.totalAmount / 100).toLocaleString()} · {new Date(order.createdAt).toLocaleDateString()}</p></Link>)}</div> : <p className="mt-3 text-sm text-slate-500">Orders placed while signed in will appear here.</p>}<Link href="/wishlist" className="mt-5 inline-block text-sm font-bold text-violet-700">View saved covers →</Link></section></div></main>;
}
