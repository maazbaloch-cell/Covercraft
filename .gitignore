node_modules
.next
.env
.env.local
.env*.local
*.log
.DS_Store

# Build artifacts
*.tsbuildinfo

# Never commit session credentials or secrets
cookies.txt
*.pem

# Temp video sources for the /track cinematic (public/track holds the shippable output)
.mediasrc/
